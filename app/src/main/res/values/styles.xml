<resources>
    <!-- Base application theme. -->
<!--    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/app_main</item>
        <item name="colorPrimaryDark">@color/app_main</item>
        <item name="colorAccent">@color/app_main</item>
        <item name="windowActionBar">false</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/app_word</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="StartTheme" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
        <item name="windowNoTitle">true</item>
        <!--        <item name="android:windowBackground">@drawable/background_img</item>-->
    </style>

    <style name="QrCodeDialog" parent="Theme.AppCompat.Light.NoActionBar">
        <!--手机顶部栏样式(显示运营商,网络信号的位置)       -->
        <item name="colorPrimaryDark">@color/transparent</item>
        <!--整个窗口的背景，包括边框和内部-->
        <item name="android:windowBackground">@color/transparent</item>
        <!--窗口是否设置为半透明-->
        <item name="android:windowIsTranslucent">true</item>
        <!--背后阴影-->
        <item name="android:backgroundDimEnabled">true</item>
<!--        &lt;!&ndash;是否浮现在activity之上&ndash;&gt;-->
<!--        <item name="android:windowIsFloating">true</item>-->
    </style>

    <!--通用的dialog-->
    <style name="CommonDialog" parent="Theme.AppCompat.Light.NoActionBar">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="SubmitDialog" parent="Theme.AppCompat.Light.NoActionBar">
        <!--手机顶部栏样式(显示运营商,网络信号的位置)       -->
        <item name="colorPrimaryDark">@color/transparent</item>
        <!--整个窗口的背景，包括边框和内部-->
        <item name="android:windowBackground">@color/transparent</item>
        <!--窗口是否设置为半透明-->
        <item name="android:windowIsTranslucent">true</item>
        <!--背后阴影-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="NoticeDialog" parent="Theme.AppCompat.Light.NoActionBar">
        <!--手机顶部栏样式(显示运营商,网络信号的位置)       -->
        <item name="colorPrimaryDark">@color/transparent</item>
        <!--整个窗口的背景，包括边框和内部-->
        <item name="android:windowBackground">@color/transparent</item>
        <!--窗口是否设置为半透明-->
        <item name="android:windowIsTranslucent">true</item>
        <!--背后阴影-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!--通用的dialog-->
    <style name="CommonDialog111" parent="Theme.AppCompat.Light.NoActionBar">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <item name="android:background">@drawable/bg_comon_dialog</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!-- 加载弹框的样式 -->
    <style name="LoadingDialog" parent="android:Theme.Dialog">
        <!-- 背景颜色及透明程度 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 是否半透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 是否没有标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否背景模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 设置背景模糊的透明度-->
        <item name="android:backgroundDimAmount">0.8</item>
    </style>

    <style name="CustomAlertDialog" parent="@style/Theme.AppCompat.Light.Dialog.Alert">
        <item name="buttonBarButtonStyle">@style/CustomAlertDialogButton</item>

    </style>

    <style name="CustomAlertDialogButton" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="textAllCaps">false</item> <!--关闭默认大写的设置 -->
        <item name="colorAccent">@color/black</item>
    </style>
</resources>
